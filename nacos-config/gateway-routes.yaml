# ==============================
# Data ID: gateway-routes.yaml
# Group  : GATEWAY_GROUP
# ==============================
spring:
  cloud:
    gateway:
      routes:
        - id: stream-service-sse-route
          uri: http://localhost:18082
          predicates:
            - Path=/stream-api/**
          filters:
            - StripPrefix=1
